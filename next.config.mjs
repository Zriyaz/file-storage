
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns:[
           {
            hostname: "youthful-duck-876.convex.cloud"
           }
        ]
    }
};

export default nextConfig;
